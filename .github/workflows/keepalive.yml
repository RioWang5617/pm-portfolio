name: Keep HF Space Alive

on:
  schedule:
    - cron: '0 */25 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -fsS ${{ secrets.SPACE_URL }}/health || echo "ping failed"
